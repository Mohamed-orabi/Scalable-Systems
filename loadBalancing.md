
# ملخص مفاهيم الأنظمة الموزعة وتوزيع الأحمال (Scalable Systems)

## 1. الجلسات اللاصقة (Sticky Sessions / Session Affinity)
- **المفهوم:** قيام موزع الأحمال (Load Balancer) بتوجيه جميع طلبات المستخدم الواحد إلى نفس الخادم (Server) طوال فترة جلسته.
- **دواعي الاستخدام:** تُستخدم عندما تكون الخوادم (Stateful)، أي تعتمد على ذاكرتها المحلية (RAM) لحفظ بيانات جلسة المستخدم.
- **عيوبها (لماذا يجب تجنبها):**
  - **توزيع أحمال غير عادل:** قد يتكدس المستخدمون النشطون على خادم واحد بينما تظل الخوادم الأخرى غير مستغلة.
  - **ضعف تحمل الأخطاء:** إذا تعطل الخادم، يفقد جميع المستخدمين المرتبطين به بيانات جلساتهم فجأة.
  - **صعوبة التوسع:** يصعب إضافة خوادم جديدة وإعادة توزيع المستخدمين الحاليين عليها.
- **البدائل الهندسية الأفضل:** جعل الخوادم (Stateless) عبر:
  1. التخزين المركزي للجلسات (Distributed Cache) مثل Redis.
  2. التخزين من جهة العميل باستخدام الـ Cookies أو (JWT).

---

## 2. فحوصات صحة الخوادم (Liveness vs Readiness Checks)
هي آليات يستخدمها موزع الأحمال أو بيئات التشغيل (مثل Kubernetes) للتأكد من حالة الخوادم قبل إرسال المستخدمين إليها.

- **فحص الحياة (Liveness Check):**
  - **الهدف:** التأكد من أن التطبيق لم يتعطل (Crash) أو يتجمد (Deadlock).
  - **السؤال:** "هل أنت حي؟"
  - **الإجراء عند الفشل:** يقوم النظام بـ **إعادة تشغيل (Restart)** الخادم أو الحاوية تلقائياً لمحاولة إنعاشه.

- **فحص الجاهزية (Readiness Check):**
  - **الهدف:** التأكد من أن الخادم مستعد لاستقبال الطلبات (أنهى تحميل البيانات أو الاتصال بقاعدة البيانات).
  - **السؤال:** "هل أنت جاهز للعمل؟"
  - **الإجراء عند الفشل:** يقوم موزع الأحمال بـ **التوقف مؤقتاً عن إرسال طلبات جديدة** لهذا الخادم، ولكنه لا يعيد تشغيله، وينتظر حتى يصبح جاهزاً.

---

## 3. التوزيع العالمي للأحمال (GSLB) وتقنيات التوجيه
تُستخدم في الأنظمة العملاقة لتوجيه المستخدمين إلى مراكز بيانات (Data Centers) موزعة حول العالم لتقليل زمن الاستجابة (Latency) وتحمل الكوارث (Disaster Recovery).

- **التوجيه الجغرافي (GeoDNS):**
  - يعتمد على خوادم الـ DNS لمعرفة الموقع الجغرافي للمستخدم (من خلال عنوان الـ IP الخاص به).
  - يرد على المستخدم بعنوان IP مختلف يخص أقرب مركز بيانات له.
- **تقنية التوجيه الأقرب (Anycast):**
  - تقنية تعتمد على إعطاء **نفس عنوان الـ IP** لعدة خوادم موزعة في دول مختلفة.
  - تقوم البنية التحتية لشبكة الإنترنت (الراوترات/BGP) بتوجيه طلب المستخدم تلقائياً إلى الخادم "الأقرب" له شبكياً بشكل فائق السرعة دون الحاجة لحساب موقعه.
