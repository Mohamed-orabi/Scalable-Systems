# اختيار مفتاح التقسيم (Shard Key) وتجنب النقاط الساخنة

تتناول هذه المقالة موضوعاً هاماً في تصميم الأنظمة القابلة للتوسع (System Design)، وهو كيفية اختيار **مفتاح التقسيم (Shard Key)** المناسب لقاعدة البيانات، وكيفية تجنب مشكلة **النقاط الساخنة (Hotspots)** باستخدام تقنيات مثل **التجميع أو الدلاء (Bucketing)** و**التقسيم العشوائي للمفاتيح (Key Randomization)**.

إليك شرح مفصل لمحتوى المقالة وأهم المفاهيم التي وردت فيها:

### 1. ما هو مفتاح التقسيم (Shard Key)؟

عندما تصبح قاعدة البيانات كبيرة جداً ولا يستطيع خادم واحد تحملها، يتم تقسيمها إلى أجزاء أصغر توزع على خوادم متعددة (عملية تُعرف بـ Sharding). لتحديد الخادم الذي سيتم تخزين أو استرجاع بيانات معينة منه، يتم استخدام عمود أو حقل معين من البيانات يُسمى "مفتاح التقسيم".

### 2. كيف تختار مفتاح تقسيم جيد؟

لكي يكون المفتاح فعالاً ولا يسبب مشاكل في المستقبل، يجب أن يتمتع بصفتين أساسيتين:

* **تعددية القيم (High Cardinality):** يجب أن يحتوي المفتاح على عدد كبير جداً من القيم الفريدة (مثلاً: استخدام "مُعرّف المستخدم User ID" يعتبر ممتازاً، بينما استخدام "البلد" سيعطيك بضع عشرات من القيم فقط مما يؤدي لتقسيم سيء).
* **توزيع متساوٍ لحركة المرور:** يجب أن يضمن المفتاح توزيع عمليات القراءة والكتابة بشكل متوازن على جميع الخوادم، بحيث لا يتم استهلاك موارد خادم واحد بينما تبقى الخوادم الأخرى فارغة.

### 3. مشكلة النقاط الساخنة (Hotspots)

تحدث "النقاط الساخنة" عندما يتركز جزء كبير من عمليات القراءة أو الكتابة على خادم واحد (Shard) دون غيره.

* **كيف تحدث؟** المثال الأشهر هو استخدام "الطابع الزمني" (Timestamp) كمفتاح تقسيم. نظراً لأن البيانات تتزايد زمنياً، فإن جميع البيانات الجديدة التي يتم إدخالها الآن ستذهب إلى خادم واحد فقط (الخادم المسؤول عن الوقت الحالي)، مما يؤدي إلى ضغط هائل عليه وبطء في النظام بأكمله، بينما باقي الخوادم لا تفعل شيئاً.

### 4. استراتيجيات حل مشكلة النقاط الساخنة

لحل هذه المشكلة، تشرح المقالة استراتيجيتين رئيسيتين:

#### أ. العشوائية في المفاتيح (Key Randomization / Hashed Shard Keys)

* بدلاً من استخدام قيمة المفتاح مباشرة لتحديد الخادم، يتم تمرير المفتاح عبر **دالة تجزئة رياضية (Hash Function)** مثل MD5.
* **الميزة:** هذه الطريقة تضمن توزيع البيانات بشكل عشوائي ومتساوٍ تماماً على جميع الخوادم، مما يقضي على مشكلة النقاط الساخنة تماماً، حتى لو كانت البيانات الأصلية متسلسلة وراء بعضها.
* **العيب الأساسي:** تجعل الاستعلامات النطاقية (Range Queries) شبه مستحيلة أو غير فعالة جداً. (على سبيل المثال، إذا أردت جلب جميع البيانات بين تاريخين معينين، ستضطر للبحث في جميع الخوادم بدلاً من خادم واحد، لأن البيانات لم تعد مخزنة بجانب بعضها بشكل متسلسل).

#### ب. تجميع البيانات أو تقسيمها إلى حزم (Bucketing / Compound Shard Key)

* هي استراتيجية ذكية تجمع بين فوائد التقسيم المتسلسل والتقسيم العشوائي. يتم ذلك عن طريق دمج مفتاح التقسيم الأصلي مع حقل إضافي (عشوائي) لإنشاء **مفتاح مركب (Compound Key)**.
* **مثال:** بدلاً من أن يكون المفتاح هو `التاريخ` فقط، نجعله `التاريخ + رقم عشوائي (من 1 إلى N)`.
* **النتيجة:** سيتم توزيع البيانات التي تحدث في نفس اليوم على $N$ خوادم مختلفة (مما يخفف الضغط عن خادم واحد ويزيل النقاط الساخنة)، وفي نفس الوقت تحتفظ بالقدرة على إجراء الاستعلامات النطاقية (Range Queries) لأنك تعرف مسبقاً أن بيانات هذا اليوم موجودة فقط ضمن هذه الخوادم المحددة (Buckets) وليس في كل مكان.

### الخلاصة

توضح المقالة أنه لا يوجد حل سحري مثالي لجميع الحالات، بل يعتمد الاختيار على احتياجات تطبيقك:

* إذا كان نظامك يحتاج إلى سرعة هائلة في القراءة والكتابة لعناصر محددة ولا يحتاج إلى البحث في نطاقات متسلسلة، فإن تقنية **Hashed Shard Keys** هي الخيار الأمثل.
* إذا كان نظامك يتطلب إجراء استعلامات نطاقية (مثل جلب بيانات آخر شهر) ولكنه يعاني من النقاط الساخنة، فإن تقنية **Bucketing** توفر أفضل توازن بين كفاءة الأداء وقدرات البحث.
